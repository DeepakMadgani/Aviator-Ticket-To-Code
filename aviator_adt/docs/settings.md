# Settings

The following settings can be used to configure the application. They need to be set as environment variables.
For local development they can be configured in the `.env` file.

--8<-- "settings_doc.md"
